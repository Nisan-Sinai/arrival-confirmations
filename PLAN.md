# תוכנית העבודה — מערכת אישורי הגעה

> מקור האמת למפרט: `~/Downloads/Arrival confirmations.docx`.
> הקובץ הזה הוא שחזור מלא שלו + מעקב התקדמות, כדי שאף סשן לא יאבד את ההקשר שוב.
> הפניות בסגנון `§4.3` בקוד ובתיעוד מתייחסות לסעיפים כאן.

---

## מצב נוכחי — היכן להמשיך

**פאזות 1–2 הושלמו. להמשיך מפאזה 3 (ליבת שרת).**

`types/database.types.ts` כבר בריפו (נוצר מהסכימה החיה), ולכן פאזה 3 לא חסומה.

**שני ערכים ב-`.env.local` עדיין חסרים ולא ניתנים להשגה אוטומטית:**
`SUPABASE_SERVICE_ROLE_KEY` ו-`TEST_DATABASE_URL`. חיבור ה-Supabase MCP חושף
מפתחות ציבוריים בלבד (`get_publishable_keys`), וסיסמת המסד אינה נחשפת אחרי יצירת
הפרויקט. הם נדרשים רק כדי **להריץ** את האפליקציה מקומית ולחבילות הטסטים שמדברות
עם Postgres ישירות (פאזה 6) — לא לכתיבת הקוד. אימות מול המסד בינתיים נעשה דרך
ה-MCP, שהוא מורשה.

### תשתית חיה

| משאב             | ערך                                                      |
| ---------------- | -------------------------------------------------------- |
| GitHub           | `Nisan-Sinai/arrival-confirmations` (פרטי)               |
| Supabase project | `qjzyjetkwqtasqzhvoat` · eu-central-1 · מסלול חינמי ($0) |
| Supabase URL     | `https://qjzyjetkwqtasqzhvoat.supabase.co`               |

`.env.local` עדיין לא קיים — המפתחות נלקחים מ-Supabase → Project Settings → API Keys
לפי הטבלה ב-SETUP.md.

### רקע: למה זה נעצר בפעם הראשונה

הסשן המקורי רץ בענן והועבר למחשב המקומי עם `messageCount: 0`, כלומר היסטוריית
השיחה אבדה בהעברה. הקבצים על הדיסק שרדו במלואם. אין כאן תקלה בקוד.

### החלטות שהתקבלו תוך כדי, ושכדאי לא לגלות מחדש

1. **שמות עמודות מוכללים.** המפרט כתב `parents_names` / `child_display_name` /
   `brit_time` (ברית מילה). בפועל: `hosts_names` / `honoree_display_name` /
   `ceremony_time`, כי המערכת תומכת ב-11 סוגי אירוע והתווית מגיעה מה-preset.
2. **ההקרנה הציבורית היא פונקציה, לא view.** `public.get_public_event()` מחזירה טיפוס
   מורכב עם רשימת עמודות קבועה, כך שהוספת עמודה ל-`events` לא יכולה להרחיב את מה
   שאנונימי רואה. אומת: 19 עמודות בטבלה, 16 חשופות, מוסתרות `created_at`,
   `updated_at`, `is_active`.
3. **הערכת RLS דורשת EXECUTE על `is_admin()`.** נבדק אמפירית מול המסד: ביטול ההרשאה
   מ-`authenticated` שובר את המדיניות עם `permission denied for function is_admin`.
   לכן ההתראה של Supabase על `is_admin`/`is_owner` הניתנות להרצה על ידי
   `authenticated` היא בלתי נמנעת ומקובלת — הפונקציה מחזירה בוליאני על הקורא עצמו
   בלבד. יש לתעד זאת ב-SECURITY.md.
4. **ה-default privileges של Supabase גוברים על `revoke from public`.** נדרשה
   מיגרציה נפרדת (`20260726000700`) כדי לבטל EXECUTE מ-`anon` בפועל.
5. **`now()` קבוע לאורך טרנזקציה** — בדיקת טריגר `updated_at` שמשווה
   `created_at` ל-`updated_at` באותה טרנזקציה תמיד "תיכשל" לשווא. הבדיקה הנכונה:
   לכתוב ערך ישן במפורש ולוודא שהטריגר דרס אותו.

---

## הנחיות נוספות מהמשתמש

מעבר למפרט המקורי, התקבלו ההנחיות הבאות ב-26/07/2026:

**מיצוב: המערכת חינמית לחלוטין.** לקחת השראה מהחוויה של שירותי אישורי ההגעה
המסחריים בישראל — כרטיס הזמנה מלוטש, טופס קצר בלי חיכוך, דשבורד עם מספרים גדולים
וברורים — אבל בלי מנוי, בלי תשלום לפי אורח ובלי הגבלת כמות. זו נקודת מכירה שצריכה
להופיע בטקסט המוצר, לא רק בקוד.

השלכות מעשיות: הכל חייב להיכנס למסלול החינמי של Vercel ושל Supabase. מכאן
ההעדפה למימוש rate limiting מעל Postgres במקום Redis בתשלום (§4.8 מתיר את שניהם),
ולהימנעות משירותים חיצוניים בתשלום לאורך כל הבנייה.

---

## §0 כללי יסוד

| כלל              | פירוט                                                                                                                                                                                                                                                                                      |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| מיקום            | תיקייה בתוך `~/Downloads`. הפרויקט כאן: `~/Downloads/arrival-confirmations`.                                                                                                                                                                                                               |
| יעד GitHub       | חשבון **אישי** בלבד, לא חשבון עבודה. ראו §18.                                                                                                                                                                                                                                              |
| אוטונומיה        | לעבוד עצמאית. לא לעצור לשאול אם אפשר להניח ולתעד. החריג היחיד: שער ה-GitHub ב-§18.                                                                                                                                                                                                         |
| הוכחה מעל הצהרה  | כל "זה עובד" מגובה בפלט אמיתי של פקודה. ראו §17.                                                                                                                                                                                                                                           |
| ללא placeholders | אין TODO/FIXME, אין מימוש מזויף, אין mocks בפרודקשן, אין פונקציות ריקות, אין תשובות הצלחה קשיחות, אין זרימות לא גמורות. ערכי אירוע כ-placeholder מותרים רק ב-`config/event.config.ts`, מתועדים ב-SETUP.md, מאומתים בעלייה, מסומנים ויזואלית מחוץ לפרודקשן, ונכשלים באימות בדפלוי לפרודקשן. |
| מונע-תצורה       | כל פרטי האירוע בקובץ תצורה אחד + טבלת `events`.                                                                                                                                                                                                                                            |
| סודות            | משתני סביבה בלבד. Service Role — צד שרת בלבד. לעולם לא לקמט `.env`.                                                                                                                                                                                                                        |
| Git              | קומיטים ממוקדים, conventional commits, היסטוריה נקייה.                                                                                                                                                                                                                                     |

### §0.1 ביצוע בפאזות — לא לעצור באמצע

לבנות בפאזות מסודרות. אחרי כל פאזה: להריץ את הבדיקות שלה, לדווח פלט אמיתי, ולהמשיך
לפאזה הבאה באותה ריצה בלי להמתין. לא לעצור אחרי scaffolding. לא לעצור אחרי יצירת
קבצים. לעצור רק בשער §18, או בחסם סביבתי אמיתי (§17).

| #   | פאזה                                                                                                                                       | סטטוס    |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------ | -------- |
| 1   | Scaffold + tooling: Next.js, TS strict, Tailwind, shadcn, ESLint, Prettier, Vitest, Playwright, `config/`, `.env.example`, אימות env       | ✅ הושלם |
| 2   | Database: migrations, enums, constraints, triggers, RLS policies, `is_admin()`, seed, טיפוסים מגוללים                                      | ✅ הושלם |
| 3   | ליבת שרת: גיבוב טוקנים, החלפת invite-session, idempotency, פונקציית submit טרנזקציונית, rate limiter + IP resolver, repositories, services | ⬜       |
| 4   | דף הזמנה ציבורי + טופס RSVP + זרימת יצירה/עדכון מאובטחת                                                                                    | ⬜       |
| 5   | אדמין: auth + דשבורד (סטטיסטיקות, גרפים, טבלה, פעולות, CSV, WhatsApp)                                                                      | ⬜       |
| 6   | טסטים: unit → component → integration → RLS → leakage → E2E → a11y → mutation                                                              | ⬜       |
| 7   | תיעוד, data-retention, ביצועים/SEO, security headers                                                                                       | ⬜       |
| 8   | §19 אימות סופי + clean-room                                                                                                                | ⬜       |
| 9   | §18 שער GitHub + push                                                                                                                      | ⬜       |

אם ההקשר אוזל: לסיים את הפאזה הנוכחית נקי, לקמט, ולציין במדויק מאיזו פאזה להמשיך.

---

## §1 סטאק

- **ליבה:** Next.js (App Router) · TypeScript strict · React · Tailwind CSS · shadcn/ui
- **נתונים:** Supabase (Postgres, Auth, RLS, Migrations, טיפוסים מגוללים)
- **טפסים:** React Hook Form + Zod (סכימה משותפת client/server)
- **Rate limiting:** Upstash Redis, KV נתמך-Vercel, או מימוש אטומי מעל Postgres
- **טסטים:** Vitest + React Testing Library + Playwright + axe-core + Stryker
- **תפעול:** Vercel · GitHub Actions · ESLint · Prettier

לבחור latest stable בזמן הבחירה בלבד. אחר כך: לנעול ב-lockfile, לקמט אותו, להתקין
עם frozen-lockfile ב-CI וב-clean-room. לתעד גרסאות Node ומנהל החבילות. אין שדרוגים
אוטומטיים במהלך האימות הסופי. חייב לרוץ על Vercel + Supabase, בלי תלות בשרת מקומי קבוע.

## §2 ארכיטקטורה

```
app/            routes (Server Components כברירת מחדל)
components/ui/  shadcn primitives
features/       rsvp/ admin/ invite/
lib/            utils, formatting, errors
lib/server/     מודולים server-only (לקוחות מורשים, לוגיקת טוקנים, IP resolver)
services/       לוגיקה עסקית
repositories/   גישה ל-DB בלבד
schemas/        Zod (מקור אמת יחיד)
types/          מגוללים + דומיין
config/         event.config.ts, messages.ts
supabase/       migrations, seed, policies, functions
tests/          unit + component + integration
e2e/            Playwright
```

**גבולות קשיחים:** UI לעולם לא נוגע ב-DB. רק `repositories/` מדבר עם Supabase.
`services/` מחזיק לוגיקה עסקית. כל קומפוננטה מעל ~200 שורות מתפצלת.
להפריד: UI · לוגיקה עסקית · גישה למסד · ולידציה · הרשאות · מיפוי · פורמט · תצורה.

## §3 מודל נתונים

```
events            id, title, parents_names, child_display_name, event_date, brit_time,
                  reception_time, venue_name, address, waze_url, google_maps_url,
                  contact_phone, description, is_active, created_at, updated_at

guests            id, event_id->events, full_name, phone, phone_normalized, family_side,
                  invite_token_hash, token_expires_at, token_revoked_at, is_active,
                  created_at, updated_at

rsvps             id, event_id->events, guest_id->guests NULL, full_name, phone,
                  phone_normalized, family_side NULL, attendance_status, adults_count,
                  children_count, babies_count, dietary_requirements, notes, consent,
                  source, update_token_hash NULL, update_token_expires_at NULL,
                  submitted_at, updated_at

admin_profiles    id, user_id->auth.users, role, created_at, updated_at

audit_logs        id, admin_user_id NULL, action, entity_type, entity_id, metadata, created_at

idempotency_keys  id, event_id->events, idempotency_key_hash, request_fingerprint,
                  response_status, response_payload, rsvp_id->rsvps NULL, created_at, expires_at

invite_sessions   id, event_id->events, guest_id->guests, session_token_hash,
                  created_at, expires_at, revoked_at
```

**אילוצים נדרשים**

- `attendance_status` → ENUM (`attending` / `not_attending` / `maybe`)
- `source` → ENUM (`personal_link` / `public_form`)
- `family_side` → ENUM או טקסט nullable מוגבל, מאומת ומנוקה בצד לקוח **וגם** בשרת
- `CHECK (adults_count >= 0 AND adults_count <= 30)`, וכנ"ל ל-children/babies
- `UNIQUE (event_id, phone_normalized)` על `rsvps` — מניעת כפילויות בלבד, **לעולם לא הרשאה** (§6.4)
- `UNIQUE (event_id, idempotency_key_hash)`
- `UNIQUE (invite_token_hash)` על `guests`; `UNIQUE (session_token_hash)` על `invite_sessions`
- אינדקסים על `phone_normalized`, `event_id`, `submitted_at`, `attendance_status`, `invite_token_hash`, `session_token_hash`, `expires_at`
- טריגר `set_updated_at()` על כל טבלה עם `updated_at`
- כל גיבובי הטוקנים ב-SHA-256. לעולם לא לאחסן טוקן גולמי או מפתח idempotency גולמי
- מסלול ניקוי בטוח למפתחות idempotency ו-invite sessions שפגו

> **כלל סמכות:** כאשר RSVP מקושר ל-guest, להגדיר ולבדוק איזה `family_side` גובר אם הם שונים.
> **מיגרציות בלבד:** כל שינוי סכימה עובר במיגרציה. לעולם לא לערוך דרך ה-UI של Supabase.

## §4 מודל אבטחה

### §4.1 מטריצת גישה ישירה למסד

| שחקן                   | גישה ישירה למסד                                                        |
| ---------------------- | ---------------------------------------------------------------------- |
| מבקר ציבורי אנונימי    | קריאה בלבד של הקרנה ציבורית בטוחה של האירוע הפעיל. שום דבר אחר.        |
| מבקר עם קישור אישי     | אין גישה ישירה לטבלאות. הכל דרך endpoint מאובטח בשרת + invite session. |
| מאומת שאינו אדמין      | אין גישה לניהול RSVP או guests.                                        |
| אדמין מאומת            | גישה לפי מדיניות RLS מפורשת לאדמין.                                    |
| פעולה מורשית בשרת בלבד | שימוש מינימלי הכרחי ב-Service Role. לעולם לא נחשף לדפדפן.              |

RLS enabled **ו-forced** על כל טבלה. לקוחות ציבוריים/אנונימיים לעולם לא כותבים ישירות
ל-`audit_logs`, `idempotency_keys`, `invite_sessions`.

### §4.2 אבטחת טוקן הזמנה אישי

**עיקרון ליבה:** טוקן הזמנה גולמי אינו יוצר role במסד ואסור להתייחס אליו כזהות RLS
מאומתת. הדפדפן לעולם לא מתשאל `guests` או `rsvps` ישירות עם טוקן גולמי.

כל טיפול בטוקן עובר דרך Route Handler או Server Action ש: מקבל את הטוקן הגולמי → מגבב
SHA-256 בשרת → מתשאל לפי `invite_token_hash` → מאמת תפוגה וביטול → מחזיר רק מידע guest
מינימלי ובטוח → מבצע עדכונים מורשים בצד שרת. RPC של Supabase עם EXECUTE מוגבל מקובל רק
אם מוכח בטוח יותר ונבדק במלואו; העדיפות לדפוס Next.js בצד שרת.

כללים: לעולם לא לחשוף `invite_token_hash` · לעולם לא לשלוח טוקן גולמי ל-Supabase מהדפדפן ·
לעולם לא בלוגים, אנליטיקס, דיווחי שגיאה או metadata של audit · לעולם לא ב-localStorage או
sessionStorage · `Cache-Control: no-store` על כל תגובות ההזמנה · `Referrer-Policy: no-referrer`
על דפי טוקן · דפי טוקן לא טוענים סקריפטים צד-שלישי שעלולים לקבל את ה-URL · טוקנים ≥32 בתים
CSPRNG → base64url · יצירה מחדש מבטלת את הקודם אטומית ומבטלת את הסשנים שלו · השוואה והרשאת
עדכון קורות באותה זרימה מאובטחת בשרת · מזהים סדרתיים אסורים כקישורים ציבוריים.

### §4.3 החלפת טוקן חד-פעמית ב-URL

טוקן גולמי ב-URL דולף להיסטוריית דפדפן, ללוגי תשתית, לצילומי מסך, לקישורים מועתקים,
ל-referrer ולניטור. לכן:

1. מבקר פותח `/invite/[rawToken]`.
2. השרת מגבב ומאמת את הטוקן.
3. השרת יוצר invite session קצר-מועד ומוגבל-מטרה.
4. מזהה הסשן נשמר בעוגייה `HttpOnly`, `Secure`, `SameSite=Lax` (או מחמיר יותר).
5. השרת מפנה מיד ל-URL ללא טוקן גולמי, למשל `/invite`.
6. כל הקריאות והעדכונים אחר כך משתמשים בסשן, לא בטוקן הגולמי.
7. תגובת ההחלפה עם `Cache-Control: no-store`.
8. דף הטוקן הגולמי לא מרנדר תוכן רגיש לפני ההפניה.
9. הסשן קשור לאירוע ול-guest ספציפיים ולא יכול להגיע להזמנה אחרת.
10. הסשן פג אוטומטית ומבוטל כשההזמנה מבוטלת או נוצרת מחדש.

> **אל:** לא לאחסן את הטוקן הגולמי בתוך העוגייה. אם URL-ים עם טוקן גולמי חייבים להישאר
> ניתנים לפתיחה חוזרת — לתעד את ה-trade-off במפורש, ועדיין להפנות מיד אחרי האימות.

### §4.4 הרשאות אדמין

ליצור פונקציית `is_admin()` במסד עם `search_path` קבוע. חייבת לאמת: משתמש Supabase מאומת
תקף · שורת `admin_profiles` פעילה ומתאימה · תפקיד מותר (`admin` / `owner`).
לא לסמוך על: ערכי role בגוף בקשה · פרמטרי query · state בלקוח · user metadata הניתן לעריכה ·
נתיב frontend מוסתר · כתובת אימייל לבדה.
למנוע: קידום עצמי · הכנסה לא מורשית ל-`admin_profiles` · שינויי תפקיד לא מורשים · הסרת ה-owner האחרון.
כל Server Action / Route Handler מבצע בדיקת הרשאה משלו. הסתרת UI אינה אבטחה. אין הרשמת
אדמין ציבורית. שגיאות התחברות גנריות (בלי user enumeration).

### §4.5 כללי מפתח מורשה של Supabase

Service Role עוקף RLS. לכן: שימוש רק במודולים server-only תחת `lib/server/` · הגנת חבילת
`server-only` · לעולם לא בקידומת `NEXT_PUBLIC_` · לעולם לא לסרלז לקוח מורשה ל-props ·
לעולם לא לחשוף בתגובת API · סריקת סודות על פלט ה-build · טסט שגורף את נכסי הלקוח המגוללים
אחרי ערכי סוד ידועים ושמות משתני סביבה מורשים · להעדיף לקוחות session של משתמש + RLS.

### §4.6 נתוני אירוע ציבוריים ותצוגות בטוחות

אנונימיים קוראים רק הקרנה ציבורית בטוחה של האירוע הפעיל. אם נעשה שימוש ב-view של Postgres,
להגדיר אותו כך שלא יעקוף את מודל ה-RLS המיועד. להעדיף: DTO מפורש מרונדר בשרת · RPC מוגבל
עם הרשאות מפורשות · או view עם התנהגות invoker-security מתאימה. לבטל גישת ברירת מחדל,
להעניק רק את הנדרש. לבדוק שלקוחות אנונימיים לא יכולים לשלוף עמודות פנימיות או חדשות דרך
ההקרנה הציבורית.

### §4.7 רישום ביקורת

נוצר רק על ידי: פונקציית מסד טרנזקציונית מאובטחת · פעולה מורשית בצד שרת · פעולת אדמין
מאומתת אחרי הרשאה בשרת.
Metadata לא יכיל: טוקנים גולמיים · סיסמאות · קודי אימות · עוגיות · כותרות authorization ·
מפתחות Service Role · גופי בקשה מלאים · כתובות IP גולמיות · מידע אישי מיותר.
סוגי אירוע ציבוריים מועדפים: `rsvp.created` · `rsvp.updated` · `invite.lookup_failed` · `rate_limit.triggered`.

### §4.8 הגבלת קצב

> **לעולם לא** להשתמש ב-Map בזיכרון, משתנה ברמת מודול, או cache מקומי לתהליך.
> פונקציות Vercel מבוזרות וארעיות.

להגביל לפחות: שליחות RSVP (גיבוב IP + גיבוב טלפון מנורמל) · חיפושי טוקן הזמנה (גיבוב IP +
גיבוב טוקן) · ניסיונות התחברות אדמין (גיבוב IP + גיבוב אימייל מנורמל) · יצירת טוקן מחדש
(מזהה אדמין מאומת).

זיהוי IP מהימן: לא לסמוך עיוור על `X-Forwarded-For` מהלקוח. לבנות IP resolver ייעודי
server-only ש: תומך בסביבת הבקשה המתועדת של Vercel · דוחה או מטפל בבטחה בכותרות פגומות ·
משתמש ב-fallback דטרמיניסטי כשאין IP מהימן · לעולם לא מתעד IP גולמי · מגבב את הערך עם pepper
בצד שרת לפני אחסון · מכוסה בטסטי unit ו-integration. לתעד הנחות אמון ב-SECURITY.md.
להחיל שמירה ותפוגה סבירות. לספק adapter פיתוח בטוח לטסטים מקומיים. לבדוק גבולות, איפוס
וקונקורנטיות. אם נבחר Redis חיצוני — לתעד כתלות תשתית ולהוסיף משתנים ל-`.env.example`.

### §4.9 כותרות אבטחה

CSP · HSTS · X-Content-Type-Options · Referrer-Policy · Permissions-Policy · `frame-ancestors 'none'`.
לתעד את מודל האבטחה המלא, כולל §4.1, ב-SECURITY.md.

## §5 דף ההזמנה הציבורי

**וייב:** הזמנה דיגיטלית יוקרתית — לא טופס טכני. לבן / תכלת / כחול עמוק / זהב. טיפוגרפיה
עברית איכותית. אנימציות עדינות בלבד, מכבדות `prefers-reduced-motion`. חגיגי אך לא עמוס.
כרטיס הזמנה מרכזי. פשוט מספיק לאורחים מבוגרים.

**תוכן:** "בשבח והודיה לה׳ יתברך" · "שמחים להזמינכם לברית המילה של בננו" · שמות ההורים ·
תאריך עברי + לועזי + יום בשבוע · שעת הברית / שעת הקבלה (אם שונות) · שם האולם, כתובת מלאה ·
כפתור Waze · כפתור Google Maps · הוספה ליומן (ICS + Google) · ספירה לאחור · פרטי קשר ·
טופס RSVP + מצב תודה.

> **תמיד:** ממשק עברי מלא. RTL מלא. Mobile-first. כל התאריכים והשעות ב-`Asia/Jerusalem`, תמיד.

## §6 טופס RSVP ושליחה

**שדות:** שם מלא\* · טלפון\* · סטטוס הגעה\* · מבוגרים · ילדים · תינוקות · הערות ·
דרישות תזונה · צד משפחה · תיבת הסכמה\*

**נרמול טלפון** (unit-tested, table-driven): `0501234567` · `050-123-4567` · `050 123 4567` ·
`+972501234567` · `972-50-123-4567` · `05-01-234-567` → `+972501234567`.
לדחות: מספרים לא ישראליים, קווי נייח לא תקינים, אורך שגוי.

### §6.1 שער טרום-טרנזקציה

ולידציית Zod בלקוח → honeypot + בדיקת תזמון → rate limit מבוזר (§4.8) → ולידציית Zod חוזרת
בשרת → sanitization.

### §6.2 שליחה טרנזקציונית — אטומית

> **הכל או כלום.** כל אלה מצליחים יחד או נכשלים יחד. להשתמש בפונקציית/טרנזקציית Postgres
> או מימוש אחר עם ערובות טרנזקציוניות אמיתיות. לא לבצע כקריאות נפרדות שיכולות להשאיר מצב חלקי.

1. לאמת זמינות אירוע. 2. לפתור הזמנה אישית / invite session אופציונליים. 3. לאמת idempotency.
2. להרשות create-vs-update לפי §6.4. 5. Upsert ל-RSVP. 6. לשייך ל-guest הנכון היכן שרלוונטי.
3. לרשום את אירוע הביקורת הבטוח. 8. לשמור את התגובה האידמפוטנטית.
   להוסיף טסטים לכשל בכל שלב ולאמת rollback.

### §6.3 Idempotency

לעולם לא לאחסן את המפתח הגולמי — לגבב. אילוץ ייחודיות על גיבוב מפתח + היקף פעולה.
אותו מפתח + אותו fingerprint → להחזיר את התוצאה המוצלחת המקורית. אותו מפתח + fingerprint
שונה → לדחות. מפתחות שפגו ניתנים להסרה בטוחה. טסטי קונקורנטיות: בקשות זהות בו-זמנית חייבות
לייצר מוטציה אחת בדיוק.

### §6.4 עדכון RSVP קיים — קריטי

> **איום:** מבקר ציבורי לעולם לא ידרוס RSVP קיים רק מתוקף ידיעת או ניחוש מספר טלפון.
> אילוץ הייחודיות מונע כפילויות; הוא **אינו** מנגנון הרשאה.

התנהגות נדרשת: שליחה ציבורית ראשונה רשאית ליצור RSVP · כשהטלפון כבר קיים, לא לחשוף זאת —
להחזיר תגובה גנרית ושומרת-פרטיות · לא להחזיר תוכן RSVP קיים למבקר לא מאומת · לא לדלוף קיום
דרך קוד סטטוס, תזמון או הודעת שגיאה · עדכון RSVP קיים מחייב אחד מ: invite session אישי תקף
(§4.3) · טוקן עדכון RSVP ייעודי ומאובטח קריפטוגרפית · קוד חד-פעמי מאומת בערוץ מאושר.
שם + טלפון לבדם לעולם אינם מספיקים לצפייה או לעדכון.
אם ממומש טוקן עדכון: ≥32 בתים CSPRNG · אחסון SHA-256 בלבד · מוגבל-אירוע ומוגבל-RSVP ·
תפוגה וביטול · רוטציה אחרי פעולות רגישות היכן שמתאים · לעולם לא בלוגים או אנליטיקס ·
לבדוק בידוד ו-replay.

> **פישוט מותר:** אם יצירה מראש של שורת `guests` לכל מוזמן (כך שכולם משתמשים בקישור אישי,
> והטופס הציבורי הוא create-only) מספקת את הערובות בפשטות רבה יותר — לבחור בדרך הזו ולתעד
> את ההחלטה ב-SECURITY.md.

**הטסטים חייבים להוכיח:** ידיעת טלפון של אורח אחר אינה מספיקה לעדכון ה-RSVP שלו · שליחות
ציבוריות כפולות לא יוצרות שורות כפולות · ניסיון כפילות לא מורשה לא דורס את המקור · טוקן
עדכון תקף מעדכן רק את ה-RSVP שהוקצה לו.

**UX:** disabled-on-submit, spinner/skeleton, טוסטים ברורים בעברית להצלחה ולשגיאה, ניסיון
חוזר בכשל רשת. לעולם לא להציג "נכשל" לשליחה שבפועל נשמרה.

## §7 קישורים אישיים — `/invite/[token]` → `/invite`

החלפת טוקן והפניה לפי §4.3 · להציג את שם האורח המוגדר מראש · לעדכן RSVP קיים דרך ה-invite
session · `noindex, nofollow` על כל נתיבי ההזמנה · לעולם לא לחשוף מזהים פנימיים ·
ניהול מהדשבורד: יצירה · העתקה · ביטול · יצירה מחדש · שיתוף ל-WhatsApp.

## §8 דשבורד אדמין — `/admin`

**אימות:** Supabase Auth (Magic Link + אימייל/סיסמה) · middleware על כל נתיבי האדמין ·
בדיקת session בשרת · בדיקת תפקיד לפי §4.4 · התנתקות · טיפול בסשן שפג · הפניות נכונות ·
אין open redirect · הגנת brute-force לפי §4.8.

**כרטיסי סטטיסטיקה:** סה"כ RSVP · משפחות/רשומות · מגיעים · לא מגיעים · מתלבטים · סה"כ
מבוגרים · סה"כ ילדים · סה"כ תינוקות · סה"כ צפויים · RSVP שהתקבלו היום · אחוז מענה (§8.1).

**גרפים:** פילוח סטטוס הגעה · RSVP ליום · מבוגרים מול ילדים · לפי צד משפחה.

**עמודות טבלה:** שם מלא · טלפון · סטטוס · מבוגרים · ילדים · תינוקות · סה"כ נוכחים · תזונה ·
הערות · צד משפחה · נשלח ב · עודכן ב · מקור.

**פעולות:** חיפוש לפי שם או טלפון · סינון לפי סטטוס · סינון לפי צד משפחה · מיון לפי
תאריך/שם/נוכחים · עימוד · עריכת RSVP · מחיקה עם דיאלוג אישור · יצירת guest · יצירת קישור
אישי · ביטול קישור · העתקת קישור · פתיחת WhatsApp עם הודעה מוכנה · רענון · עדכונים
אופטימיים רק היכן שבטוח · טוסטים · רישום ביקורת לכל פעולת אדמין.

> **מובייל:** בנייד הטבלה הופכת לכרטיסים — אסור שתישבר.

**ייצוא:** CSV עם `﻿` UTF-8 BOM כדי שעברית תיפתח נכון ב-Excel. ייצוא נפרד לרשימת
דרישות התזונה.

### §8.1 אחוז מענה — לא להמציא

```
אחוז מענה = רשומות guest מוזמנות שהשיבו / סה"כ רשומות guest מוזמנות פעילות
```

לספור רק guests של האירוע הנוכחי. להגדיר ולתעד מפורשות אם הזמנות מבוטלות או לא-פעילות
מוחרגות. RSVP ציבורי ללא רשומת guest תואמת עשוי להופיע בסך ה-RSVP אך אסור שישנה בשקט את
המכנה. אם אין רשימת guests — להציג "לא זמין", לעולם לא להמציא אחוז. לתעד את החישוב המדויק.
לבדוק: אפס guests · מענה חלקי · מענה מלא · guests מבוטלים · תגובות מהטופס הציבורי בלבד.

### §8.2 הודעת WhatsApp

ניתנת לעריכה ב-`config/messages.ts`, מקודדת ל-URL כראוי:

```
בשבח והודיה לה׳ יתברך, אנו שמחים להזמינכם לברית המילה של בננו.

נשמח שתעדכנו אותנו לגבי הגעתכם בקישור:
[PERSONAL_INVITE_URL]

מחכים לחגוג יחד ❤
```

## §9 נגישות — WCAG 2.2 AA

HTML סמנטי · `lang="he"` · `dir="rtl"` · H1 יחיד עם היררכיית כותרות נכונה · labels לכל
השדות · הודעות שגיאה נגישות · `aria-live` · ניהול פוקוס · מצבי פוקוס נראים · ניווט מקלדת
מלא · skip link · יחסי ניגודיות · alt text · דיאלוגים נגישים · טבלאות נגישות · זום 200% ·
מסכים קטנים · תמיכת קורא מסך · `prefers-reduced-motion`.

> **שער קשיח:** סריקת axe בכל דף ב-Playwright. אפס הפרות זה שער קשיח.

## §10 טסטים

### §10.1 כיסוי: 100% / 100% / 100% / 100%

Statements · Branches · Functions · Lines. הסף בקובץ התצורה. CI נכשל ב-99.99%.
**היקף:** 100% חל על כל לוגיקת האפליקציה הניתנת להרצה שנכתבה: לוגיקה עסקית · ולידציה ·
הרשאות · עזרי אבטחה · מיפוי · פורמט · repositories · services · Server Actions ·
Route Handlers · קומפוננטות אינטראקטיביות שנכתבו.
**החרגות מותרות, מוגבלות ל:** טיפוסי Supabase מגוללים · קבצי framework מגוללים · קוד
shadcn/צד-שלישי מגולל ולא-משונה · קבצי `.d.ts` טהורים · מיגרציות SQL · קבצי תצורה ללא
לוגיקת אפליקציה.

> **לא ניתן למשא ומתן:** כל החרגה מפורטת בנפרד ב-TESTING.md עם הצדקה. דוח 100% גלובלי
> שמחריג קוד משמעותי שנכתב — לא מקובל.
>
> **אסור:** `/* istanbul ignore */` · `/* c8 ignore */` · החרגת לוגיקה עסקית · החרגת קוד
> קשה-לבדיקה במקום לבדוק אותו · הורדת הסף · טסטים חסרי משמעות שרק מריצים שורות ·
> over-mocking שעוקף התנהגות אמיתית · מחיקת ענפים כדי לשחק עם המדד · הצהרת 100% בלי דוח מגולל.

דוחות: text · html · lcov · json.

### §10.2 Unit

סכימות Zod · נרמול טלפון · ולידציית טופס · חשבון נוכחים · סטטיסטיקות · חישוב אחוז מענה כולל
מקרה אפס-guests · URL של WhatsApp · URL של ICS/יומן · פורמט תאריכים · עזרי הרשאה · mappers ·
טיפול בשגיאות · לוגיקת idempotency · כלי טוקנים (יצירה/גיבוב/אימות) · לוגיקת invite session ·
IP resolver · לוגיקת rate-limit · ייצוא CSV · כלי אבטחה.

### §10.3 Component

טופס RSVP · מצבי שגיאה · מצבי טעינה · הודעת הצלחה · החלפת סטטוס · שדות מונים · ולידציה נגישה ·
טבלת אדמין · חיפוש · סינון · דיאלוג מחיקה · מצב ריק · כרטיסי דשבורד · מצב "לא זמין" של אחוז
מענה · כרטיסי מובייל · טופס התחברות.

### §10.4 Integration

יצירת RSVP · ניסיון עדכון לא מורשה לפי טלפון נדחה ולא דורס · עדכון מורשה דרך invite session ·
עדכון מורשה דרך טוקן עדכון · מניעת כפילויות · טוקן תקף/לא תקף/מבוטל/פג · בידוד טוקנים
(A לא מגיע ל-B) · בידוד סשנים · הרשאת אדמין · משתמש לא מורשה נחסם · repositories · Server
Actions · Route Handlers · מדיניות RLS · דליפת עמודות בהקרנה הציבורית · ייצוא CSV · מחיקת
RSVP · audit logs · rollback טרנזקציוני בכל שלב · בקשות אידמפוטנטיות זהות במקביל → מוטציה
אחת בדיוק · גבולות rate-limit, איפוס, קונקורנטיות.

### §10.5 E2E (Playwright)

**אורח מגיע:** פתיחת הזמנה → צפייה בפרטים → מילוי שם וטלפון → בחירת מגיע → הזנת מספרים →
שליחה → הצלחה → אימות שורה ב-DB → התחברות אדמין → אימות בדשבורד → אימות עדכון סטטיסטיקות.
**לא מגיע:** מספרים מאופסים/מטופלים נכון, מאומת בדשבורד.
**מתלבט:** בחירת maybe → שמירה → עדכון מאוחר למגיע → אימות שאין שורה כפולה.
**קישור אישי:** פתיחת `/invite/[token]` → אימות הפניה ל-URL ללא טוקן גולמי → הצגת שם האורח →
שליחה → פתיחה חוזרת → עדכון → אימות בדשבורד.
**שגיאות ואבטחה:** שדות חסרים · טלפון לא תקין · מספרים שליליים · מספרים אבסורדיים · טוקן
מזויף · טוקן שפג · טוקן מבוטל · טוקן של אורח אחר · סשן של אורח אחר · ניסיון עדכון עם טלפון
של מישהו אחר בלבד · `/admin` בלי התחברות · משתמש מחובר שאינו אדמין · גישה ישירה ל-API ·
קריאת RSVP של אורח אחר · SQL injection · XSS בהערות · שליחה כפולה מהירה · רענון אחרי שליחה ·
כשל DB זמני · רשת איטית · viewport נייד · ניווט מקלדת בלבד · סריקת axe.
**אדמין:** התחברות → צפייה → חיפוש → סינון → מיון → עריכה → יצירת guest → יצירת קישור →
העתקה → ביטול → מחיקה → ייצוא CSV → התנתקות → חזרה ל-`/admin` חסומה.

### §10.6 RLS — טסטים אמיתיים מול מסד אמיתי, לא mocks

להוכיח עם לקוחות Supabase אמיתיים: anon לא יכול לעשות SELECT ל-`rsvps`, `guests`,
`audit_logs`, `idempotency_keys`, `invite_sessions` · anon לא יכול DELETE או UPDATE ל-RSVP ·
anon קורא רק את ההקרנה הציבורית הבטוחה · מאומת שאינו אדמין לא ניגש לנתוני הדשבורד · אדמין
יכול לקרוא ולנהל · משתמש לא יכול לקדם את עצמו לאדמין · משתמש לא יכול להכניס ל-`admin_profiles` ·
מפתח ה-Service Role לעולם לא מופיע ב-bundle של הלקוח.

### §10.7 טסטי דליפה

להוכיח שהטוקן הגולמי **לא** מופיע ב: לוגי אפליקציה · audit logs · payloads של אנליטיקס ·
תגובות API · HTML מרונדר · אחסון דפדפן · ה-URL אחרי ההפניה. להוכיח שעוגיית ההזמנה היא
HttpOnly, Secure בפרודקשן, ובהיקף נכון.

### §10.8 Mutation Testing

Stryker על לוגיקת הליבה: ולידציה · סטטיסטיקות · נרמול טלפון · הרשאות · לוגיקת טוקנים ·
idempotency · rate limiting · ייצוא CSV.
**אסטרטגיית הרצה:** ריצות ממוקדות על לוגיקת ליבה שהשתנתה ב-PR; החבילה המלאה לפני האימות
הסופי, לפי בקשה, או ב-workflow מתוזמן. הפרדת הריצה המלאה מ-CI מהיר מותרת רק אם דוח המוטציה
הסופי עדיין מיוצר, ה-Definition of Done לא יכול לעבור בלעדיו, כשלים מדווחים בכנות, ו-CI
הרגיל עדיין מריץ כל טסט חובה אחר.

### §10.9 בדיקות נוספות

Type check · lint · format check · build · security audit · dependency audit · סריקת סודות
(מקור **וגם** פלט build) · גריפת סודות ב-bundle של הדפדפן · זיהוי קוד מת ו-exports לא בשימוש.

## §11 CI/CD

GitHub Actions על כל push ו-pull_request:

1. Checkout · 2. Setup Node (גרסה נעוצה) · 3. התקנת תלויות (frozen lockfile) · 4. Cache ·
2. אימות סביבה · 6. Prettier --check · 7. ESLint · 8. `tsc --noEmit` · 9. טסטי unit ·
3. טסטי component · 11. טסטי integration · 12. טסטי RLS (DB טסטים ייעודי/מקומי) ·
4. סף כיסוי (100%) · 14. טסטי מוטציה ממוקדים · 15. Build · 16. סריקת סודות בפלט ה-build ·
5. התקנת דפדפני Playwright · 18. טסטי E2E · 19. טסטי נגישות · 20. security + dependency audit ·
6. העלאת artifacts בכשל: playwright-report · screenshots · videos · traces · דוח כיסוי.

> **שער מיזוג:** CI אדום = אין מיזוג. לתעד הגנת ענף מומלצת ב-README.

**סביבת טסטים:** Supabase Local או פרויקט טסטים ייעודי. לעולם לא פרודקשן. seed דטרמיניסטי.
ניקוי לפני ואחרי כל חבילה. שמות וטלפונים ייחודיים. אין תלות בסדר הטסטים. אין טסטים
מתנדנדים. אין sleeps קשיחים — auto-waiting של Playwright בלבד. חזרתי ומריץ ב-CI במלואו.

## §12 ביצועים ו-SEO

Metadata עברית מלאה · Open Graph עם תמונת שיתוף מותאמת ל-WhatsApp (1200x630) · favicon ·
robots.txt · sitemap.xml רק אם מתאים · `noindex` על `/admin` וכל נתיבי `/invite` · תמונות
ממוטבות · טעינה מהירה · אין layout shift · Server Components כברירת מחדל · lazy loading ·
אסטרטגיית cache נכונה · revalidation אחרי מוטציות אדמין · Error Boundary · not-found · דף
שגיאה מקצועי · דף offline בסיסי אם אינו מסבך את המערכת.
יעד: ציוני Lighthouse גבוהים ב-Performance, Accessibility, Best Practices, SEO.

## §13 טיפול בשגיאות ולוגים

טיפול עקבי בשגיאות · לעולם לא לחשוף stack traces · לעולם לא לדלוף מידע רגיש · הודעות
ידידותיות בעברית · לוגים בטוחים בצד שרת (לעולם לא טוקנים, סיסמאות, PII או IP גולמי) ·
קודי שגיאה פנימיים · טיפול בכשלי Supabase, timeouts, נתונים פגומים, ונתק רשת באמצע שליחה.

## §14 שמירת נתונים

ליצור ולתעד תהליך שמירה למידע האישי של האורחים: תקופת שמירה ניתנת להגדרה אחרי האירוע ·
ייצוא אדמין לפני מחיקה · מחיקה או אנונימיזציה של טלפונים, הערות, מידע תזונתי, טוקנים,
invite sessions, רשומות idempotency ומזהי rate-limit כשאינם נחוצים · כללי שמירה מפורשים
ל-audit logs · פעולת אדמין מוגנת או פקודת מסד מתועדת לניקוי · אישור לפני ניקוי הרסני ·
טסטים המוכיחים שהניקוי משפיע רק על האירוע הנבחר ולא שובר דבר.

> **פרטיות:** לעולם לא לכלול מידע אמיתי של אורחים ב-seeds, fixtures, צילומי מסך, לוגים,
> תיעוד, artifacts של CI או דוחות טסטים.

## §15 דפלוי וסקריפטים

לספק: `.env.example` (כולל משתני ספק ה-rate-limit) · אימות env עם Zod בעלייה · דפלוי
פרודקשן נכשל אם פרטי אירוע חובה עדיין placeholders · יצירת פרויקט Supabase · פקודות מיגרציה ·
יצירת משתמש אדמין · הקצאת תפקיד · הגדרת דומיין · שלבי דפלוי ל-Vercel · משתני פרודקשן ·
משתני preview · rollback · גיבוי · שחזור · רוטציית מפתחות.

> **בטיחות:** לוודא שדפלויי Preview לעולם לא מתחברים למסד הפרודקשן.

סקריפטים: `dev` · `build` · `start` · `lint` · `lint:fix` · `format` · `format:check` ·
`typecheck` · `test` · `test:watch` · `test:unit` · `test:integration` · `test:rls` ·
`test:coverage` · `test:e2e` · `test:e2e:ui` · `test:a11y` · `test:mutation` ·
`test:mutation:full` · `scan:secrets` · `check` · `ci` · `supabase:start` · `supabase:stop` ·
`supabase:reset` · `supabase:types` · `data:retention:cleanup`.
`pnpm ci` מריץ את שער האיכות המלא.

## §16 תיעוד

README.md מלא בעברית: תיאור המערכת · צילומי מסך (או placeholders) · ארכיטקטורה · מבנה
תיקיות · דרישות · התקנה · משתני סביבה · הגדרת Supabase · מיגרציות · יצירת אדמין · הרצה
מקומית · הרצת טסטים · הרצת E2E · יצירת כיסוי · דפלוי ל-Vercel · אבטחה · RLS · גיבוי ושחזור ·
שמירת נתונים · פתרון תקלות · רשימת פקודות.
בנוסף: SECURITY.md · TESTING.md · DEPLOYMENT.md · ARCHITECTURE.md · SETUP.md.

## §17 ביצוע כן — חובה

> **כלל ליבה: לעולם לא לזייף הרצה מוצלחת.**

אם פעולה אינה ניתנת לביצוע כי credentials, Docker, Supabase CLI, גישת GitHub, גישת Vercel,
גישת רשת או תלות אחרת אינם זמינים: להשלים כל מה שאפשר · להריץ את כל הבדיקות שהסביבה תומכת
בהן · לכלול את הפקודה המדויקת שנוסתה · לכלול את פלט הכשל הרלוונטי · לציין במדויק מה נותר
לא מאומת · לא להחליף תוצאות integration או E2E אמיתיות בהצלחה מדומה · לא להצהיר ש-CI ירוק
אלא אם ריצת GitHub Actions אמיתית הושלמה בהצלחה · לא להצהיר שדפלוי הצליח אלא אם URL פרוס
אמיתי אומת.

## §18 שער בטיחות GitHub — חשבון אישי בלבד

> **החריג היחיד** לעבודה ללא שאלות.

להריץ `gh auth status` — לתפוס hostname וחשבון מאומתים. להריץ ולהציג (עם מיסוך טוקנים):
`git config --global user.name` · `--global user.email` · `--local user.name` · `--local user.email`.
לדחוף רק כשהחשבון האישי חד-משמעי. אם החשבון הוא חשבון חברה, לא ידוע, דו-משמעי או לא
מאומת — לעצור לפני יצירת ריפו ודחיפה, ולשאול. לעולם לא להחליף חשבונות GitHub אוטומטית.
לעולם לא לשנות תצורת Git גלובלית; להשתמש ב-`git config --local` בלבד. לפני דחיפה לאמת את
בעל ה-remote עם `git remote -v`.
אחר כך: לאמת ש-`.gitignore` מכסה `.env*`, `node_modules`, `.next`, `coverage`,
`playwright-report`, `test-results`, `.vercel` · להריץ סריקת סודות מלאה — לבטל אם נמצא משהו ·
ליצור ריפו **פרטי** בחשבון האישי · היסטוריית קומיטים נקייה ו-conventional · לדחוף `main` ·
לאמת ש-GitHub Actions הופעל ולדווח את הסטטוס האמיתי · למסור את כתובת הריפו.

> **לעולם לא** לקמט סודות אמיתיים, טלפונים אמיתיים או נתוני אורחים אמיתיים.

## §19 אימות פרודקשן סופי

להריץ ולדווח תוצאות אמיתיות של: התקנת תלויות נקייה מ-frozen lockfile · format check · lint ·
type check · טסטי unit · component · integration · טסטי RLS מול מסד טסטים אמיתי · כיסוי
(ארבעת המדדים) · חבילת מוטציה מלאה · build פרודקשן · סריקת סודות בפלט + גריפת סודות
ב-bundle · Playwright E2E · טסטי נגישות axe · dependency audit · זיהוי קוד מת ו-exports לא
בשימוש · `git status` נקי.

**אימות clean-room:** לשכפל או להעתיק את הריפו לתיקייה זמנית חדשה · להתקין עם frozen
lockfile · להגדיר רק את משתני סביבת הטסטים המתועדים · לאפס ולזרוע את מסד הטסטים · להריץ את
פקודת ה-CI המלאה · להוכיח שהריפו עובד בלי קבצים מקומיים לא מוצהרים או מצב מכונה נסתר.

## Definition of Done

- [ ] הפרויקט נוצר בתיקיית Downloads
- [ ] האתר הציבורי עובד
- [ ] טופס ה-RSVP שומר נתונים אמיתיים
- [ ] השליחה טרנזקציונית באמת, עם טסטי rollback
- [ ] Idempotency מוכח תחת קונקורנטיות
- [ ] Rate limiting מבוזר, עם IP resolver מהימן בצד שרת
- [ ] ידיעת מספר טלפון לא יכולה לעדכן או לחשוף RSVP של אחר — מוכח בטסט
- [ ] אין שורות כפולות בשליחה חוזרת
- [ ] קישורים אישיים עובדים דרך החלפת טוקן; אין טוקן גולמי ב-URL אחרי ההפניה — מוכח בטסט
- [ ] בידוד טוקנים וסשנים מוכח בטסט
- [ ] דשבורד האדמין עובד
- [ ] ל-`is_admin()` יש `search_path` קבוע; קידום עצמי בלתי אפשרי, מוכח בטסט
- [ ] RLS פעיל ומוכח מול מסד אמיתי
- [ ] ההקרנה הציבורית של האירוע לא מדליפה עמודות פנימיות, מוכח בטסט
- [ ] מפתח Service Role נעדר מ-bundle הלקוח, מוכח בטסט
- [ ] אחוז המענה נכון, ומציג "לא זמין" ללא רשימת guests
- [ ] חיפוש, סינון ומיון עובדים
- [ ] ייצוא CSV נפתח נכון ב-Excel עם עברית
- [ ] רספונסיבי מלא
- [ ] נגישות עוברת (axe: אפס הפרות)
- [ ] אפס שגיאות TypeScript · אפס שגיאות ESLint · Build עובר
- [ ] Unit / Component / Integration / RLS / E2E — כולם עוברים
- [ ] כיסוי 100% אמיתי בארבעת המדדים, דוח מגולל, ללא החרגות משמעותיות
- [ ] חבילת מוטציה מלאה הורצה, הציון דווח בכנות
- [ ] CI ירוק (ריצת GitHub Actions אמיתית)
- [ ] אין סודות במקור או בפלט ה-build
- [ ] אין TODO / FIXME / קוד מת
- [ ] Lockfile מקומט; clean-room מתקין את אותו גרף תלויות
- [ ] תהליך שמירת נתונים מתועד ונבדק
- [ ] README וכל המסמכים שלמים
- [ ] אימות clean-room עבר
- [ ] מוכן לדפלוי ב-Vercel
- [ ] נדחף ל-GitHub האישי, ריפו פרטי
